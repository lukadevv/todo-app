import { createTheme } from "@mui/material";

const darkTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#e35d10",
    },
    secondary: {
      main: "#2e9bdd",
    },
  },
});

export default darkTheme;
